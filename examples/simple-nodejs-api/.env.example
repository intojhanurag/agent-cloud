# Server Configuration
PORT=3000
NODE_ENV=development

# Cloud Provider (set automatically during deployment)
CLOUD_PROVIDER=local
CLOUD_REGION=local

# API URL for testing
API_URL=http://localhost:3000
